<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.EvenementController"
            stylesheets="/styles/rsrv.css"
            styleClass="root">

    <left>
        <VBox styleClass="sidebar" spacing="16" prefWidth="240">
            <VBox spacing="6" styleClass="sidebarHeader">
                <Label text="EcoAdventure" styleClass="brand"/>
                <Label text="Admin" styleClass="mutedSidebar"/>
            </VBox>

            <VBox spacing="8" styleClass="sidebarNav">
                <Button text="🎫  Événements" onAction="#loadEvents" styleClass="navItem navItemActive"/>
                <Button text="📅  Réservations" onAction="#goToReservations" styleClass="navItem"/>
            </VBox>

            <Region VBox.vgrow="ALWAYS"/>

            <Separator styleClass="sidebarSep"/>
            <Button text="⎋  Déconnexion" styleClass="navItem danger"/>
        </VBox>
    </left>

    <top>
        <HBox styleClass="topbar" spacing="12" alignment="CENTER_LEFT">
            <Button text="← Accueil" onAction="#goHome" styleClass="ghostBtn"/>
            <Label text="Événements" styleClass="pageTitle"/>

            <Region HBox.hgrow="ALWAYS"/>

            <HBox styleClass="searchBox" spacing="8" alignment="CENTER_LEFT">
                <Label text="🔎" styleClass="muted"/>
                <TextField fx:id="searchField" promptText="Rechercher..." styleClass="searchInput"/>
            </HBox>

            <Button text="Rechercher" onAction="#onSearch" styleClass="secondaryBtn"/>
            <Button text="Actualiser" onAction="#onRefresh" styleClass="ghostBtn"/>
            <Button text="＋ Nouveau" onAction="#onAdd" styleClass="primaryBtn"/>
        </HBox>
    </top>

    <center>
        <VBox styleClass="page" spacing="14">
            <padding>
                <Insets top="18" right="18" bottom="18" left="18"/>
            </padding>

            <VBox styleClass="card" spacing="12" VBox.vgrow="ALWAYS">
                <HBox alignment="CENTER_LEFT">
                    <Label text="Liste des événements" styleClass="cardTitle"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Label fx:id="lblInfo" text="Total: 0" styleClass="mutedText"/>
                </HBox>

                <ScrollPane fitToWidth="true" hbarPolicy="NEVER" vbarPolicy="AS_NEEDED"
                            styleClass="transparentScroll"
                            VBox.vgrow="ALWAYS">
                    <content>
                        <VBox fx:id="cardsContainer" spacing="12" styleClass="cardsContainer">
                            <padding>
                                <Insets top="5" right="10" bottom="5" left="5"/>
                            </padding>
                        </VBox>
                    </content>
                </ScrollPane>
            </VBox>
        </VBox>
    </center>

</BorderPane>